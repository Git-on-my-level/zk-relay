{
  "name": "zk-relay",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Zero-knowledge secret sharing for people and agents.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check src/worker.js && node --check src/secret-object.js && node --check public/protocol.js && node --check public/crypto.js && node --check public/app.js",
    "test:js": "node --test tests/*.test.js",
    "test:go": "GOCACHE=${GOCACHE:-/tmp/zk-relay-go-cache} go test ./...",
    "test": "npm run test:js && npm run test:go",
    "dev": "npx --yes wrangler@4.41.0 dev",
    "deploy": "npx --yes wrangler@4.41.0 deploy"
  },
  "devDependencies": {
    "wrangler": "4.41.0"
  }
}
